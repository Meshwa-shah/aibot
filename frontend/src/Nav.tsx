import React from "react";
import { toast } from "react-toastify";
import axios from "axios";
import { useNavigate } from "react-router-dom";

export const Nav: React.FC = () => {
   
  
     
    return (
        <div className="w-full h-14 flex justify-end px-4">
           
        </div>
    );
};
